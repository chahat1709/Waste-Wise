# Public Supabase values are safe to expose to the browser. Do not put a service role key here.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Server-only integration configuration (add only in the server/API deployment environment).
# WASTE_WISE_TELEMETRY_INGESTION_SECRET=replace-with-a-long-random-secret
# ROUTING_PROVIDER=mappls-or-here-after-surat-spike
# ROUTING_PROVIDER_API_KEY=server-side-only
